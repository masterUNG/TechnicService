import 'package:flutter/material.dart';

class WidgertImage extends StatelessWidget {
  const WidgertImage({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}